<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wecuit.mybatis.sub">
    <select id="selectFirst" resultType="hashmap">
        SELECT * FROM wc_sub_template WHERE tpl_client = #{client}
    </select>
</mapper>